services:
  db:
    image: mysql:latest
    container_name: local-mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
      - MYSQL_DATABASE=${DB_NAME}
    volumes:
      - D:/Document/FASTCAMPUS/DOCKER/db:/var/lib/mysql
